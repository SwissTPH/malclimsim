% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted_model_assessment.R
\name{assess_model_performance}
\alias{assess_model_performance}
\title{Assess Model Performance}
\usage{
assess_model_performance(observed_df, simulated_df, date_column, groups)
}
\arguments{
\item{observed_df}{Data frame containing the observed data.}

\item{simulated_df}{Data frame containing the simulated data.}

\item{date_column}{The name of the date column in both data frames.}

\item{groups}{A character vector specifying which groups to assess (e.g., \code{c("inc_A", "inc_C", "inc")}).}
}
\value{
A list containing a data frame of error metrics and a ggplot object of residual plots.
}
\description{
This function assesses the performance of a model by calculating error metrics and plotting residuals.
}
\examples{
# Example using minimal mock data
obs_cases <- data.frame(
  date_ymd = as.Date("2022-01-01") + 0:6,
  inc_A = rpois(7, lambda = 10),
  inc_C = rpois(7, lambda = 12),
  inc = rpois(7, lambda = 15)
)

simulated_df <- obs_cases

assess_model_performance(obs_cases, simulated_df, date_column = "date_ymd", groups = c("inc_A", "inc_C", "inc"))
}
