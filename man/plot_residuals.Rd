% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted_model_assessment.R,
%   R/model_visualizations.R
\name{plot_residuals}
\alias{plot_residuals}
\title{Plot Residuals of Observed vs Simulated Data}
\usage{
plot_residuals(
  observed_df,
  simulated_df,
  date_column,
  groups = c("inc_A", "inc_C", "inc")
)

plot_residuals(
  observed_df,
  simulated_df,
  date_column,
  groups = c("inc_A", "inc_C", "inc")
)
}
\arguments{
\item{observed_df}{Data frame containing the observed data.}

\item{simulated_df}{Data frame containing the simulated data.}

\item{date_column}{The name of the date column in both data frames.}

\item{groups}{A character vector specifying which groups to include in the plot (\code{inc_A}, \code{inc_C}, \code{inc}).}
}
\value{
A ggplot object displaying the residual plots.

A ggplot object displaying the residual plots.
}
\description{
This function creates residual plots for observed and simulated data, allowing the user to select specific groups.

This function creates residual plots for observed and simulated data, allowing the user to select specific groups.
}
\examples{
residual_plot <- plot_residuals(obs_cases, simulated_df, date_column = "date_ymd", groups = c("inc_A", "inc_C"))
residual_plot <- plot_residuals(obs_cases, simulated_df, date_column = "date_ymd", groups = c("inc_A", "inc_C"))
}
