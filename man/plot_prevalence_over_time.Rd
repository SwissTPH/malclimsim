% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_visualizations.R
\name{plot_prevalence_over_time}
\alias{plot_prevalence_over_time}
\title{Plot Estimated and True Prevalence Over Time}
\usage{
plot_prevalence_over_time(
  prevalence_data,
  prevalence_true = NULL,
  true_types = "prev",
  districts = NULL,
  prevalence_types = NULL,
  title = "Estimated and True Prevalence Over Time",
  color_palette = "Set2"
)
}
\arguments{
\item{prevalence_data}{A data frame containing \code{year}, \code{District}, \code{Prevalence_Type}, and \code{Value} columns.}

\item{prevalence_true}{(Optional) A data frame containing observed prevalence estimates with columns \code{District}, \code{Year}, \code{prev_A}, \code{prev_C}, \code{prev}.}

\item{true_types}{A character vector specifying which of the true prevalence values to include (\code{prev_A}, \code{prev_C}, \code{prev}). Default is \code{prev}.}

\item{districts}{A character vector specifying which districts to include. Default is all.}

\item{prevalence_types}{A character vector specifying which prevalence types to include. Default is all.}

\item{title}{A character string specifying the plot title.}

\item{color_palette}{A character string specifying the color palette from RColorBrewer (default: "Set2").}
}
\value{
A ggplot2 object visualizing the prevalence trends over time.
}
\description{
This function creates a time series plot of estimated malaria prevalence.
Prevalence types are plotted on the same facet, while districts are shown as separate facets.
True prevalence estimates (from MAP data) can also be included and selected (\code{prev_A}, \code{prev_C}, \code{prev}).
}
\details{
\itemize{
\item The function allows users to compare model-estimated prevalence against true values from malaria prevalence data (MAP).
\item True prevalence data can be plotted for \strong{all age groups (\code{prev}), only adults (\code{prev_A}), or only children (\code{prev_C})}.
\item If \code{prevalence_true} is provided, it is plotted as \strong{circles} (\code{shape = 16}), while model estimates are displayed as \strong{lines + dots}.
\item If \code{prevalence_true} is NULL, only model estimates are plotted.
}
}
\examples{
# Plot only model estimates
plot_prevalence_over_time(prevalence_model_estimated)

# Plot with true prevalence values (default = total prevalence)
plot_prevalence_over_time(prevalence_model_estimated, prevalence_true)

# Compare model estimates to only `prev_A` (adults)
plot_prevalence_over_time(prevalence_model_estimated, prevalence_true, true_types = "prev_A")

# Compare selected prevalence types
plot_prevalence_over_time(prevalence_model_estimated, prevalence_true,
                          prevalence_types = c("prev_total_with_R", "prev_A_with_R"),
                          true_types = c("prev_A", "prev_C"))

}
