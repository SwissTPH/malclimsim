% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_visualizations.R
\name{plot_simulation_confidence_intervals}
\alias{plot_simulation_confidence_intervals}
\title{Plot Confidence Intervals for Simulated Compartments}
\usage{
plot_simulation_confidence_intervals(
  summary_df,
  obs_data = NULL,
  plot_type = "line",
  compartments = NULL,
  plot_proportion = FALSE,
  log_scale = FALSE,
  title = "Confidence Intervals for Simulated Compartments",
  color_palette = "Set2",
  y_label = "Value"
)

plot_simulation_confidence_intervals(
  summary_df,
  obs_data = NULL,
  plot_type = "line",
  compartments = NULL,
  plot_proportion = FALSE,
  log_scale = FALSE,
  title = "Confidence Intervals for Simulated Compartments",
  color_palette = "Set2",
  y_label = "Value"
)

plot_simulation_confidence_intervals(
  summary_df,
  obs_data = NULL,
  plot_type = "line",
  compartments = NULL,
  plot_proportion = FALSE,
  log_scale = FALSE,
  title = "Confidence Intervals for Simulated Compartments",
  color_palette = "Set2",
  y_label = "Value"
)
}
\arguments{
\item{summary_df}{A data frame containing summarized simulation results with confidence intervals.}

\item{obs_data}{A data frame containing observed data with columns \code{date_ymd}, \code{prev_A}, and \code{prev_C}.}

\item{plot_type}{Character string specifying the type of plot. Options are "line", "stacked", or "facet".}

\item{compartments}{Optional character vector of compartments to plot (e.g., c("SC", "IC")).}

\item{plot_proportion}{Logical indicating whether to plot proportions relative to the total population (\code{P} column).}

\item{log_scale}{Logical indicating whether to apply a logarithmic scale to the y-axis.}

\item{title}{Character string specifying the plot title (default: "Confidence Intervals for Simulated Compartments").}

\item{color_palette}{Character string specifying the color palette from RColorBrewer (default: "Set2").}

\item{y_label}{Character string specifying the Y-axis label (default: "Value").}
}
\value{
A ggplot object.

A ggplot object.

A ggplot object.
}
\description{
This function generates a time series plot of median simulated values with confidence intervals,
supporting different plot types, proportion scaling, and log scaling, with consistent ribbon and line colors.

This function generates a time series plot of median simulated values with confidence intervals,
and overlays observed prevalence data as dots with consistent coloring and a clear legend.

This function generates a time series plot of median simulated values with confidence intervals,
and overlays observed prevalence data as dots with consistent coloring and a clear legend.
}
\examples{
# Basic line plot
plot_simulation_confidence_intervals(summary_df, plot_type = "line")

# Stacked area plot with selected compartments
plot_simulation_confidence_intervals(summary_df, plot_type = "stacked", compartments = c("SC", "IC"))

# Faceted plot with proportions and log scale
plot_simulation_confidence_intervals(summary_df, plot_type = "facet", plot_proportion = TRUE, log_scale = TRUE)
}
