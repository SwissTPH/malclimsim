% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_aux_functions.R
\name{index}
\alias{index}
\title{Create Index Mapping for Model Variables}
\usage{
index(info)
}
\arguments{
\item{info}{A list containing index information, typically provided from a model setup.
It must include \code{info$index$...} entries for each variable named below.}
}
\value{
A list with two named elements:
\itemize{
\item \code{run}: Named integer vector of indices for running the model (monthly and weekly incidence, prevalence, and rates)
\item \code{state}: Integer vector of state indices, in a predefined order matching model state variables.
}
}
\description{
This function creates a list of indices for different model variables,
organizing them into \code{run} and \code{state} categories based on the provided info.
}
\examples{
# Create a dummy info object with index entries
info <- list(
  index = as.list(setNames(seq_along(c(
    "month_inc_C", "month_inc_A", "month_inc_total",
    "wk_inc_C",   "wk_inc_A",    "wk_inc_total",
    "prev_C_2",   "prev_A_2",    "prev_C_1",
    "prev_A_1",   "r_C",         "r_A"
  )),
  paste0("index$", c(
    "month_inc_C", "month_inc_A", "month_inc_total",
    "wk_inc_C",   "wk_inc_A",    "wk_inc_total",
    "prev_C_2",   "prev_A_2",    "prev_C_1",
    "prev_A_1",   "r_C",         "r_A"
  ))))
)

# Extract mapping
model_indices <- index(info)
str(model_indices)

}
