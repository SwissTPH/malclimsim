% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_distributions.R
\name{build_priors}
\alias{build_priors}
\title{Build a list of mcstate::pmcmc_parameter() objects,
starting from the return_default_priors() template and then layering on any overrides.}
\usage{
build_priors(
  param_inputs,
  proposal_matrix,
  params_to_estimate,
  override_priors = NULL
)
}
\arguments{
\item{param_inputs}{Named list of initial values (so that we know which parameters actually exist).}

\item{proposal_matrix}{Numeric matrix (rownames must match parameter names).}

\item{params_to_estimate}{Character vector of names we actually want to estimate.}

\item{override_priors}{Optional named list of lists: each element must match the structure
returned by return_default_priors(). If you supply an element "phi" here,
it replaces the entire \code{phi} block in the default.}
}
\value{
Named list of \code{mcstate::pmcmc_parameter} objects, one per parameter in \code{params_to_estimate}.
}
\description{
Build a list of mcstate::pmcmc_parameter() objects,
starting from the return_default_priors() template and then layering on any overrides.
}
